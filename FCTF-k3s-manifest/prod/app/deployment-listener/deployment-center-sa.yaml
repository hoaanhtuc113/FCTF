apiVersion: v1
kind: ServiceAccount
metadata:
  name: deployment-listener-sa
  namespace: app
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: deployment-listener-sa-role
rules:
  - apiGroups: [""]
    resources: ["pods", "services", "namespaces", "nodes"]
    verbs: ["get", "list", "watch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: deployment-listener-sa-binding
subjects:
  - kind: ServiceAccount
    name: deployment-listener-sa
    namespace: app
roleRef:
  kind: ClusterRole
  name: deployment-listener-sa-role
  apiGroup: rbac.authorization.k8s.io
