apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: app
  name: deployment-center
spec:
  replicas: 3
  selector:
    matchLabels:
      app: deployment-center
  template:
    metadata:
      labels:
        app: deployment-center
    spec:
      serviceAccountName: deployment-center-sa
      priorityClassName: app-normal-preemption
      tolerations:
        - key: "node-role.kubernetes.io/control-plane"
          operator: "Exists"
          effect: "NoSchedule"
      containers:
        - name: deployment-center
          image: <REGISTRY_DOMAIN>/fctf/deployment-center:latest
          ports:
            - name: http-client
              containerPort: 5020
              protocol: TCP
          envFrom:
            - configMapRef:
                name: deployment-center-cm
            - secretRef:
                name: deployment-center-secret
            - secretRef:
                name: common-private-key-secret
          env:
            # CA that signed the Redis server certificate. Without it the
            # service falls back to the container trust store, which does not
            # know fctf-internal-ca, and the connection fails - certificate
            # validation is on by default now.
            - name: REDIS_TLS_CA_FILE
              value: /etc/redis-ca/ca.crt
          resources:
            requests:
              cpu: 0.2
              memory: 256Mi
            limits:
              cpu: 0.5
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 5020
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 5020
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          imagePullPolicy: Always
          securityContext:
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1654
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            privileged: false
            capabilities:
              drop:
                - NET_RAW
            seLinuxOptions:
              type: container_t
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: redis-ca
              mountPath: /etc/redis-ca
              readOnly: true
      imagePullSecrets:
        - name: regcred
      volumes:
        # Only ca.crt - the leaf and key in this secret are for a later mTLS
        # switch and nothing here uses them yet.
        - name: redis-ca
          secret:
            secretName: redis-client-tls
            items:
              - key: ca.crt
                path: ca.crt
  revisionHistoryLimit: 3
