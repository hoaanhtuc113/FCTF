# Copy of common-private-key-secret.yaml's PRIVATE_KEY, in the argo
# namespace: K8s Secrets are namespace-scoped, and the up-challenge-template
# workflow's send-message step (which now signs its callback to
# DeploymentCenter's /api/statuscheck/message) runs in namespace argo, not
# app. Keep this value in sync with common-private-key-secret.yaml if the
# key is ever rotated.
apiVersion: v1
kind: Secret
metadata:
  name: common-private-key-secret
  namespace: argo
stringData:
  PRIVATE_KEY: "emdungdepzai_secret_key_fctf_platform_2025"
