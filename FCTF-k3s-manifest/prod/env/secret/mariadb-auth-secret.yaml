apiVersion: v1
kind: Secret
metadata:
  name: mariadb-auth-secret
  namespace: db
type: Opaque
stringData:
  # Use unique, high-entropy passwords. Recommended length >= 24.
  #
  # Placeholder, not a value: apply-fctf.sh substitutes it in memory and pipes
  # the result to kubectl, so no root password is committed here and none is
  # written to disk at install time. Never `kubectl apply -f` this file
  # directly - that stores the literal below as the password.
  mariadb-root-password: "${MARIADB_ROOT_PASSWORD}"
  mariadb-password: "5od4k6jgcmobfavNtTCddADlum1agrRUPOhKA0LA3WFiYxckWz"
  # Required by the chart even in standalone mode.
  mariadb-replication-password: "VAT1zd53iPs8yC2CReYvyyCksQZGbsFX0fqazyso1MkeywIBIP"
