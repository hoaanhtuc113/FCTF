apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: harbor-tls
  namespace: registry
spec:
  secretName: harbor-tls
  dnsNames:
    - registry.fctf.site
  issuerRef:
    name: letsencrypt-dev
    kind: ClusterIssuer