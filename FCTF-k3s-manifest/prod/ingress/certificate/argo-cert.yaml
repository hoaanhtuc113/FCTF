apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argo-tls
  namespace: argo
spec:
  secretName: argo-tls
  dnsNames:
    - argo.fctf.site
  issuerRef:
    name: letsencrypt-dev
    kind: ClusterIssuer