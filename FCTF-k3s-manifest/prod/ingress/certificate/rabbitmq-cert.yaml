apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rabbitmq-tls
  namespace: db
spec:
  secretName: rabbitmq-tls
  dnsNames:
    - rabbitmq.sanchoi.iahn.hanoi.vn 
  issuerRef:
    name: letsencrypt-dev
    kind: ClusterIssuer