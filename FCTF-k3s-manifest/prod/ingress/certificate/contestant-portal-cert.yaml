apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: contestant-tls
  namespace: app
spec:
  secretName: contestant-tls
  dnsNames:
    - sanchoi.iahn.hanoi.vn
  issuerRef:
    name: letsencrypt-dev
    kind: ClusterIssuer