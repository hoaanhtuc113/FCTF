apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sonarqube-tls
  namespace: sonarqube
spec:
  secretName: sonarqube-tls
  dnsNames:
    - sonarqube-zmn0zjiwmju.fctf.cloud
  issuerRef:
    name: letsencrypt-dev
    kind: ClusterIssuer