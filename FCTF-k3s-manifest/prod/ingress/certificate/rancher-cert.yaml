apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rancher-tls
  namespace: cattle-system
spec:
  secretName: rancher-tls
  dnsNames:
    - rancher0.fctf.site 
  issuerRef:
    name: letsencrypt-dev      
    kind: ClusterIssuer
