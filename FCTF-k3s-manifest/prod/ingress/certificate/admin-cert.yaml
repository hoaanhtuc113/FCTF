apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: admin-tls
  namespace: app
spec:
  secretName: admin-tls
  dnsNames:
    - admin.sanchoi.iahn.hanoi.vn
  issuerRef:
    name: letsencrypt-dev
    kind: ClusterIssuer