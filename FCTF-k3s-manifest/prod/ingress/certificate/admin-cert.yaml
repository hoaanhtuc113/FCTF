apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: admin-tls
  namespace: app
spec:
  secretName: admin-tls
  dnsNames:
    - admin0.fctf.site
  issuerRef:
    name: letsencrypt-dev
    kind: ClusterIssuer