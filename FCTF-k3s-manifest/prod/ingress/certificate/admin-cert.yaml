apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: admin-tls
  namespace: app
spec:
  secretName: admin-tls
  dnsNames:
    - <ADMIN_DOMAIN>
  issuerRef:
    name: letsencrypt-dev
    kind: ClusterIssuer